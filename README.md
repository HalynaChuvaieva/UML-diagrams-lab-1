# Лабораторна робота №1 "Рефакторінг та моделювання з використанням UML"
## 📖 Глосарій предметної галузі (Domain Glossary)

### 1. Базові структури даних (Data Structures)
* **Структура даних (Data Structure):** Спосіб організації, зберігання та управління даними в комп'ютері для їх ефективного використання та модифікації.
* **Зв'язний список (Linked List):** Лінійна структура даних, елементи якої не зберігаються у суміжних ділянках пам'яті. Кожен елемент містить самі дані та посилання (вказівник) на наступний елемент.
* **Стек (Stack):** Лінійна структура даних, що працює за принципом LIFO (Last In, First Out — останнім прийшов, першим пішов). Додавання (`push`) та видалення (`pop`) елементів відбувається лише з одного кінця (вершини).
* **Черга (Queue):** Лінійна структура даних, що працює за принципом FIFO (First In, First Out — першим прийшов, першим пішов). Елементи додаються в кінець (`enqueue`), а вилучаються з початку (`dequeue`).

### 2. Дерева (Trees)
* **Бінарне дерево пошуку (Binary Search Tree / BST):** Ієрархічна структура даних, що складається з вузлів. Кожен вузол має не більше двох нащадків (лівого та правого). Значення в лівому піддереві завжди менші за корінь, а в правому — більші.
* **АВЛ-дерево (AVL Tree):** Збалансоване бінарне дерево пошуку, в якому для кожного вузла висота його двох піддерев відрізняється не більше ніж на 1. Підтримує баланс за допомогою операцій обертання (`rotateLeft`, `rotateRight`).
* **Вузол (Node):** Базовий елемент дерева або зв'язного списку, який містить значення (корисні дані) та посилання на інші вузли.
* **Корінь (Root):** Найвищий (початковий) вузол дерева, який не має батьківського вузла.
* **Листок (Leaf):** Вузол дерева, який не має жодного нащадка.

### 3. Графи (Graphs)
* **Граф (Graph):** Нелінійна структура даних, що складається з множини вершин (вузлів) та множини ребер, які ці вершини з'єднують.
* **Вершина (Vertex):** Фундаментальна одиниця графа, що репрезентує об'єкт або сутність.
* **Ребро (Edge):** Зв'язок між двома вершинами графа.
* **Орієнтований граф (Directed Graph):** Граф, у якому ребра мають напрямок (від однієї вершини до іншої).
* **Зважений граф (Weighted Graph):** Граф, кожному ребру якого присвоєно певне числове значення — вагу (наприклад, відстань або вартість).
* **Цикл (Cycle):** Шлях у графі, який починається і закінчується в одній і тій самій вершині.
* **Остове дерево (Spanning Tree):** Підграф, який містить усі вершини початкового графа, але є деревом (тобто не містить циклів).

### 4. Терміни візуалізації та архітектури
* **Стан (State):** Поточний знімок даних додатка у визначений момент часу (включає набір елементів у структурі, поточний крок алгоритму тощо).
* **Історія станів (History):** Масив збережених попередніх станів структури даних, що дозволяє користувачеві "подорожувати в часі" (крокувати назад і вперед).
* **Обхід (Traversal):** Процес систематичного відвідування всіх вузлів дерева або вершин графа (наприклад, BFS — пошук в ширину, DFS — пошук в глибину, Inorder — симетричний обхід).
* **Анімація переходів (Layout Animation):** Візуальне плавне переміщення елементів інтерфейсу (вузлів, блоків) з однієї позиції в іншу під час зміни стану структури даних (реалізовано за допомогою Framer Motion).
